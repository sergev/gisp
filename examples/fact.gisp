#!/usr/bin/env gisp

func fact(n) {
	if n == 0 {
		return 1
	}
	return n * fact(n - 1)
}

func fact_tr(n, acc) {
	if n == 0 {
		return acc
	}
	return fact_tr(n - 1, acc * n)
}

display(fact(5))
newline()

display(fact_tr(5, 1))
newline()
