#!/usr/bin/env gisp

func accumulateUntil(limit) {
	var sum = 0.0
	var term = 1.0

	while sum < limit {
		sum = sum + term;
		term = term / 2.0;
	}
	return sum;
}

display(accumulateUntil(2.0));
newline();

