#!/usr/bin/env gisp

func compose(f, g) {
    return func(x) {
        return f(g(x))
    }
}

func increment(n) { return n + 1; }
func double(n) { return n * 2; }

display(compose(increment, double)(10))
newline()
