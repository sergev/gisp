#!/usr/bin/env gisp

func compose(f, g) {
    return func(x) {
        return f(g(x))
    }
}

var increment = func(n) { return n + 1; }
var double = func(n) { return n * 2; }

display(compose(increment, double)(10))
newline()
