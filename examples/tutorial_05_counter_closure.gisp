#!/usr/bin/env gisp

func counter(start) {
    var value = start
    return func(step) {
        value += step
        return value
    }
}

var c = counter(10)
display(c(1))
newline()
display(c(5))
newline()
