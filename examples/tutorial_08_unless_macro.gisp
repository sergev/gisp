#!/usr/bin/env gisp

`(define-macro (unless condition . body)
    (list 'if condition '#f (cons 'begin body)))

func demo(value) {
    `(unless value
        (display "value was false\n"))
}

demo(false)
demo(true)
