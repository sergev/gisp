#!/usr/bin/env gisp

func classify(n) {
    if n < 0 {
        return "negative"
    } else {
        if n == 0 {
            return "zero"
        }
    }
    return "positive"
}

display(classify(-3))
newline()
display(classify(0))
newline()
display(classify(8))
newline()
